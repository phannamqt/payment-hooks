version: '3.9'

services:
  app:
    image: phannamit/payment-hooks:latest
    build: .
    container_name: payment-hook-app
    ports:
      - '3010:3000'
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
